# Visual Verification

## 2026-08-24

桌面版已確認採用深色、高對比、分區明確的優雅儀表板版面。綜合分數、regime、置信度和短期變化位於首屏；五項因子、資料頻率與來源披露均可直接查看。已將走勢圖時間軸固定為分類型，避免時間字串被當作數值位置處理。

行動版已確認使用單欄卡片編排。主要狀態、情境切換、五項因子、切換紀錄與資料新鮮度保持可讀，沒有橫向溢出。

## 2026-08-25 — 實際資料引擎

桌面版已成功顯示伺服器取得的實際市場快照，而非示範資料。驗證時的實際結論為綜合分數 +38、狀態中性、置信度 87%。介面已逐項展示 Nasdaq NDX 延遲報價、FRED 的 VIX／高收益債利差／美元指數及 CoinGecko 比特幣資料，並保留頻率、來源和原始時間戳。首個快照已成功保存；尚待建立部署後的背景刷新排程並驗證多次自動保存。

正式網域已驗證可載入真實市場狀態。資料查詢在初始骨架畫面後完成，顯示綜合分數 +35、中性、置信度 86%，並已呈現五個已保存的實際快照。正式頁面每 60 秒重新查詢一次目前結論。

每分鐘背景刷新已驗證成功。排程工作 `market-regime-refresh` 對正式網站的 `/api/scheduled/refresh-market-regime` 回呼於 2026-08-25T13:57:22Z 開始，HTTP 200，耗時 943ms，並保存一個新市場快照（結論：中性）。程式會在來源失敗時把個別因子改為不可用、以中性分數處理，並降低整體置信度。

行動版正式驗證成功。初始載入後，窄螢幕呈現綜合分數 +34、中性、置信度 86%，並能完整顯示自動更新政策、實際分數走勢、五因子資料卡、最近結論紀錄及資料新鮮度披露，沒有橫向溢出。

## 2026-08-25 — 手動刷新穩定性修復

根本原因為連續強制刷新同時向多個外部來源發出請求，部分 FRED 請求會在 15 秒內逾時，進而以不可用狀態覆蓋原本正常的因子。本次修正加入來源分級快取、進行中請求去重、FRED 請求節流及資料庫最近成功快照備援。修正後，在正式網站連續觸發五次手動刷新，每次當前快照均為 `fresh`、不可用因子為 0、備援因子為 0。背景刷新亦在 14:22 及 14:23 UTC 成功回呼並取得 HTTP 200。

背景回歸驗證已完成。查詢 14:23:38 UTC 由 Heartbeat 保存的最新資料庫快照，`dataStatus` 為 `fresh`，五個因子均有有效資料，沒有 `unavailable` 或 `stale` 因子；當次結論為中性、分數 +30、置信度 84%。

## 2026-08-25 — 三市場擴展

桌面版已驗證全球、香港及中國三條路由可分別載入五因子結論及三市場比較卡。驗證時，三個實際快照均有五個因子：全球 +29、香港 +11、中國 +26，均為中性。香港頁以 EWH、KWEB、USD/CNY、VIX 與 HY OAS 表示；中國頁以 ASHR、KWEB、USD/CNY、VIX 與 HY OAS 表示。行動版亦確認市場切換、代理披露、因子卡及資料新鮮度可在單欄中完整顯示，沒有橫向溢出。

正式環境驗證完成。三市場總覽端點回傳全球 +31、香港 +10、中國 +23，三者均為完整資料覆蓋、五個因子皆可用。Heartbeat 在 2026-08-25T15:24:18Z 成功以新格式更新全球、香港與中國三個快照。正式香港頁面已確認可載入比較卡、五項代理因子、獨立走勢及資料披露；香港結論為中性 +10、置信度 66%。

全球頁行動版已在資料快取暖啟動後完成驗證：三市場比較卡、全球綜合分數、驅動因素、走勢、五因子卡片、近期結論與資料新鮮度均能在 390px 寬度完整呈現，沒有橫向溢出。至此全球、香港及中國三個頁面的行動版均已驗證。

## 2026-08-26 — 免費公開輔助確認

已驗證 OFR FSI 官方資料端點能以 HTTP 範圍請求取得最新觀察，並以 6 小時快取使用；其只在 FSI 高於 0 時把置信度下調 2 至 6 點，從不改寫五因子綜合分數。HKEX Stock Connect 官方每日檔案已驗證可取得 SSE／SZSE 北向及南向總成交；因資料是總成交活躍度而非淨買入／賣出，程式只展示為中港跨境參與度背景，不影響分數或置信度。Cboe 公開 Index Put/Call 歷史檔案被核實為舊檔，沒有納入自動化結論。

本機三市場刷新驗證為：全球 +32、香港 +9、中國 +27；OFR FSI 為 -0.563，對置信度影響為 0；香港及中國皆取得 Stock Connect 北向 267,421.21、南向 92,982.04 的日度總成交。三個頁面均已顯示「免費公開輔助確認」區塊，明確標注它們不改變主分數。

正式全球頁驗證完成。部署後首次版本化載入完成後，頁面顯示 OFR FSI -0.563、標籤為「不改主分數」，並披露為日度、約兩個工作日延遲；全球五因子綜合分數仍為 +32、置信度 85%。這確認慢速 OFR 資料沒有被誤作即時主因子。

## 2026-08-26 — 日終資料延遲分類修正

診斷確認提示並非外部請求逾時，而是 FRED 的 Trade Weighted Dollar Index 與 USD/CNY 日終序列因採用曆日判斷而被過早標示為 stale。新規則按交易日計算：發布後兩個交易日內為 fresh、三至五個交易日披露為 delayed、超過五個交易日才視為 stale。回歸測試涵蓋週末；刷新後全球、香港及中國均為 `dataStatus=fresh`，沒有 stale 或 unavailable 因子，美元及 USD/CNY 皆為 fresh。

修正後連續執行五次強制手動刷新：全球、香港及中國每次均為 `dataStatus=fresh`；Dollar index 與 USD/CNY 每次均為 fresh；置信度穩定於全球 86%、香港 68%、中國 85%。

正式環境最終驗證完成。部署傳播後，正式強制刷新回傳全球、香港與中國均為 `dataStatus=fresh`；Dollar index 與 USD/CNY 均為 fresh，沒有 stale 或 unavailable 因子。OFR FSI 及 HKEX Stock Connect 輔助訊號同樣為 fresh，兩者的綜合分數影響均為 0，確認補充資料不會降低主結論的完整性。

輔助訊號保護測試完成。OFR 來源 unavailable 時，該輔助訊號的置信度及綜合分數影響均為 0，五因子主分數保持不變；OFR 壓力訊號（−6 置信度）與 Stock Connect 中性訊號並存時，只會按上限把置信度由 85% 調整至 79%，綜合分數仍維持原值。測試套件現共有 15 項測試全部通過。

## 2026-08-26 — 多時間區間走勢

三市場 API 已驗證可返回 1 小時、1 日、1 週及全部歷史的實際快照資料；目前資料庫回傳全球 339 個、香港與中國各 271 個真實快照。圖表以 1H 分鐘級、1D 每 15 分鐘、1W 每小時及全部歷史每日最後觀察彙整，沒有生成或回填虛構數據。全球桌面版及香港行動版均已驗證時間軸按鈕、區間變化／高低位摘要、圖表與資料口徑可讀且無橫向溢出。

完成網址同步後，桌面版已逐一直接載入 1H、1D、1W、全部歷史：四個視圖皆正確啟用對應標籤，圖表觀察數、區間變化與高低位摘要均按區間更新。香港市場行動版的 1D 視圖同樣成功顯示實際走勢與摘要，沒有橫向溢出或骨架停留。

可檢查的頁面文字驗證：全球 1D 視圖顯示「每 15 分鐘保留一個實際觀察」、50 個圖表觀察、區間變化 -3、高位 +34、低位 +29，以及實際時間標籤。頁面已不在骨架載入狀態，並顯示所有資料來源可用。

全球 1W 視圖的頁面文字顯示「每小時保留最後一個實際觀察」、14 個圖表觀察、區間變化 -3、高位 +34、低位 +30。全部歷史視圖顯示「每日保留最後一個實際觀察」、2 個圖表觀察、區間變化 -1、高位 +32、低位 +31。兩個視圖均完成資料載入並顯示所有資料來源可用。

全球 1H 視圖的頁面文字顯示「保留短線的分鐘級節奏」、14 個圖表觀察、區間變化 0、高位 +31、低位 +30。香港 1D 視圖則顯示 46 個圖表觀察、區間變化 +3、高位 +20、低位 +7，且所有資料來源可用。網址解析規則現有單元測試覆蓋 1H、1D、1W、全部及無效值回退；測試套件合共 18 項全部通過。

## 2026-08-26 — Cloudflare 完整遷移（進行中）

Cloudflare D1 `market-regime-pulse` 已建立，Worker `market-regime-pulse` 亦已成功發佈並啟用 workers.dev。首次公開載入確認 Worker 可以提供前端和同源 API；但頁面仍載入舊版四個時間標籤，且 FRED／CoinGecko 資料未成功取得，故此版本尚未完成驗收。下一輪會重新建置前端、重發佈 Worker，並診斷 Cloudflare 出站資料請求結果。

Cloudflare 公開網址為 `https://market-regime-pulse.lumahub.workers.dev`，D1 資料庫識別碼為 `e47ed68a-ea2d-45ed-af9b-413d920a8707`。Worker API 的實際刷新診斷顯示 FRED VIX／HY OAS／美元序列回傳 HTTP 520，CoinGecko 回傳 HTTP 403；Nasdaq 與 OFR FSI 則可取得資料。這些來源結果是 Cloudflare 出站請求環境的實測，尚待調整來源或備援方式。

重發佈只顯示 1H／1D 的前端後，公開網址可取得新的 HTML title，但瀏覽器畫面為空白，故尚未完成驗收。下一步必須先查看瀏覽器主控台與靜態資產路徑，修正後才可再測兩個時間範圍。

診斷結果顯示新版 `index-Cf4TYv4g.js` 與 `index-5xNfbcp4.css` 已由 Cloudflare 正常回應，React 根節點亦已包含儀表板內容，且沒有瀏覽器主控台錯誤。先前空白截圖屬載入時機問題，需等待畫面穩定後再擷取驗證。

套用時間軸可見性修正後的公開頁面已取得新版骨架畫面；此刻市場 API 尚在載入，未可把該截圖視為成功驗收。後續需等待實際資料完成載入，再確認控制項及 1H／1D 摘要。

Cloudflare 可靠代理版本的實際 `POST /api/refresh` 顯示 VXX（波動）、HYG（信貸）、UUP（美元）與 IBIT（跨資產）均為可用的 15 分鐘延遲資料，已取代先前的 FRED 520／CoinGecko 403。當次 NDX 回應缺少完整欄位，程式依原有保護策略使用最近成功值，因此全球頁為部分資料覆蓋、置信度 56%。

同一次三市場強制刷新顯示各頁均有五個可用或備援因子。全球的新 VXX／HYG／UUP／IBIT 代理已生效；香港及中國的 CYB、KWEB 當次尚未取得完整新報價，故因子鍵仍沿用舊 FRED／KWEB 最近成功值。此為備援鍵值按因子名稱對應的結果，需等待或修正新來源的完整報價後，才能把來源標示完全轉換。

中港 UUP 修正版本已通過本機型別檢查、18 項測試及建置；但 API 發佈呼叫超出服務時限而未回傳確認，因此仍需以公開 API 的實際輸出確認是否已生效。一次公開頁面主控台診斷因 JavaScript 物件括號錯誤而未能執行，未對資料或部署產生任何影響。

Cloudflare Cron Trigger 已設定為 `* * * * *`，每分鐘呼叫 Worker 的 scheduled handler 保存三市場快照。原網站最後 24 小時共 1,277 個實際快照亦已成功搬進 D1：全球 483、香港 457、中國 457；最早觀察約為 2026-08-25 14:31 UTC。香港 `?range=1d` 公開頁面已進入骨架載入，尚需等待 API 完成以取得最終可檢查文字證據。

香港公開 `?range=1d` 完成載入後，頁面文字確認只有 1H、1D 兩個控制項、95 個 1D 實際圖表觀察、區間變化 -32、高位 +20、低位 -21，並且沒有停留在骨架。頁面顯示 EWH、VXX、HYG、KWEB 15 分鐘延遲資料及 HKEX／OFR 輔助訊號；人民幣因子目前維持最近成功的 FRED 備援，公開頁清楚標示為最近成功值及部分資料覆蓋。

最終 Worker 發佈於 2026-08-26 14:40 UTC 成功完成。重新開啟香港 1D 公開網址時先出現正常資料骨架，後續需等待同源 API 解析完成，驗證新部署的文案及 UUP 代理。

最終香港 1D 公開頁完成載入後，確認所有資料來源可用，五因子為 EWH、VXX、HYG、UUP、KWEB，均清楚標示 Nasdaq 15 分鐘延遲與實際時間戳；不再使用 FRED 最近成功值。走勢控制項只顯示 1H、1D，資料口徑文字也只列出「1H 保留分鐘觀察、1D 每 15 分鐘取最後實際值」。該頁顯示 95 個真實 1D 觀察、區間變化 -36、高位 +20、低位 -25。並已從公開頁按下「立即刷新全部」，介面確認進入「更新三市場中」狀態。

手動刷新完成後，按鈕已恢復為「立即刷新全部」，香港分數由 -25 更新至 -26、計算時間更新至 22:43 HKT；EWH 及 KWEB 時間戳推進至 10:43 AM ET。D1 同時顯示三市場各有新快照（14:42 UTC，全球 494、香港 468、中國 468），符合每分鐘 Cron 已實際寫入的證據。

最終全球首頁 `?range=1h` 已重新載入並進入正常資料骨架；待同源 Cloudflare API 回應後補記全球五因子、分鐘級摘要及手動刷新證據。

QQQ 修正後的全球首頁已確認所有資料來源可用；五因子為 QQQ、VXX、HYG、UUP、IBIT，皆為 Nasdaq 15 分鐘延遲資料且無最近成功值備援。全球 1H 視圖只顯示 1H、1D，並有 48 個真實觀察、區間變化 -36、高位 +26、低位 -11；最新快照為完整資料覆蓋、置信度 81%。

最終中國 1D 頁亦確認所有資料來源可用；五因子為 ASHR、VXX、HYG、UUP、KWEB，全部使用 Nasdaq 15 分鐘延遲資料，沒有舊 FRED 或最近成功值。頁面只顯示 1H、1D，並有 96 個實際 1D 觀察、區間變化 -23、高位 +45、低位 +3。連同已驗證的全球及香港頁，三市場五因子遷移已完成。

最終全球 `?range=1h` 手動刷新驗證頁已重新載入並進入正常資料骨架，待同源 API 回應後直接按下刷新按鈕。

最終全球首頁載入後顯示 QQQ、VXX、HYG、UUP、IBIT 五個可用因子，49 個 1H 實際觀察、區間變化 -36、高位 +26、低位 -10，以及只含 1H、1D 的控制項。按下「立即刷新全部」後按鈕立即改為「更新三市場中」，確認手動刷新請求已從最終首頁送出。

最終全球首頁的手動刷新已完成，按鈕恢復為「立即刷新全部」，畫面維持全部資料來源可用、五個 QQQ／VXX／HYG／UUP／IBIT 因子、49 個 1H 實際觀察及僅含 1H、1D 的控制項；刷新流程沒有破壞區間摘要或資料披露。

刷新後 D1 查詢顯示三市場最新快照同為 2026-08-26 14:51:49.526 UTC；全球 513 筆、香港 487 筆、中國 487 筆，較此前 14:42 UTC 的各市場數量均增加。此為首頁手動刷新及每分鐘排程均確實把三市場新結論寫入 D1 的最終可檢查證據。

為排除 Cron 影響，最終首頁直接呼叫手動刷新 API，回傳三市場相同的 `calculatedAt`：2026-08-26 14:53:03.136 UTC（全球 -9、中國 +3、香港 -26，均為 fresh）。隨即以該精確時間查詢 D1，取得三個相同市場、分數與 fresh 狀態的儲存列，證明此批快照直接由首頁手動刷新保存。

Manus 正式入口在未保存新修正前仍載入舊前端，故仍顯示 HTML 當 JSON 解析錯誤。本機同源 Cloudflare 轉接測試等待逾時而被中止；需要改用可直接驗證的入口設定或加上明確逾時處理，避免正式入口長時間懸掛。

Cloudflare 已加入只允許 Manus 正式網域的跨來源 JSON 回應。從 Manus 正式入口執行公開 API 讀取，取得 HTTP 200、`application/json; charset=utf-8` 及回應首字元 `{`；前端現會在非 Cloudflare 網域直接讀取該 JSON，並會先檢查格式，避免 HTML 再被誤解析。

保存並重新載入最新 Manus 正式站版本後，公開頁面仍顯示舊的 `Unexpected token '<'` 錯誤，表示正式網域尚未使用新前端取數資產或請求仍被既有路由覆蓋。下一步要直接檢查該頁載入的 JavaScript 資產及實際請求網址。

資產診斷顯示正式站仍載入 `index-BHFnCHGZ.js`，且該資產不含 `market-regime-pulse.lumahub.workers.dev` 或舊 `/api/overview` 常量，證實它不是本次修正後的前端建置。需等待或觸發正式站資產更新後再驗證。

正式站資產傳播完成後，`https://mriskdash-y5e2kzgw.manus.space` 已成功載入實際市場儀表板，沒有 `Unexpected token '<'`。全球首頁顯示所有資料來源可用、QQQ／VXX／HYG／UUP／IBIT 五因子、52 個 1H 真實觀察、1H／1D 控制項與正確資料口徑，證實新 Cloudflare JSON 讀取已生效。

在已修正的 Manus 正式首頁按下「立即刷新全部」後，按鈕即時顯示「更新三市場中」，完成後恢復「立即刷新全部」。畫面繼續顯示五因子、52 個實際 1H 觀察及 1H／1D 控制項，未再出現 HTML JSON 解析錯誤。Cloudflare 公開入口此前已完成相同資料、時間軸與手動刷新驗證。

同源版本發佈後，Manus 正式網址在約 8 秒後仍停留在資料骨架而沒有顯示錯誤，表示 `/api/overview` 請求仍未完成；需直接診斷正式 API 的回應時間與備援流程。

直接由正式頁面呼叫同源 `/api/overview?range=1h` 已取得 HTTP 200、`application/json; charset=utf-8` 及 JSON 內容；因此服務端已可取數，現正處理已載入頁面初始查詢未解除骨架的用戶端狀態。

資產檢查顯示正式頁目前載入 `index-aXZXBQGj.js`，其中仍含 Cloudflare URL；這是前一版跨來源前端，而非剛發布的同源取數版本，故仍可能出現 `Failed to fetch`。需待新資產傳播後再作正式驗證。

同源版本資產傳播後，正式網址已成功由骨架進入完整儀表板，沒有 `Failed to fetch`。全球頁顯示所有資料來源可用、五個 QQQ／VXX／HYG／UUP／IBIT 因子、50 個 1H 實際觀察、1H／1D 控制項、分數 -10 及 11:30 ET 快照時間。

快速備援版本已再度在正式網址成功載入：全球分數 -9、三市場比較完整、五個來源可用、51 個 1H 實際觀察、1H／1D 控制項及 11:33 ET 最新快照時間均可檢查，沒有 `Failed to fetch`。

快速備援版本的手動刷新可進入「更新三市場中」，但十多秒後仍未完成，顯示等待上游 `refresh` 回應本身仍可能過久。將改為即時回傳已保存快照，同時於背景觸發上游刷新，確保介面不會卡住。

背景刷新回應版本已在正式網址成功載入：分數 -12、五個來源及三市場比較可見、51 個 1H 實際觀察、1H／1D 控制項、最新 11:36 ET 快照均正常，並無 `Failed to fetch`。

在同一正式頁按下手動刷新後，按鈕由「更新三市場中」恢復為「立即刷新全部」，畫面仍顯示三市場、五因子、1H／1D，且全球分數更新為 -13，過程未出現 `Failed to fetch`。最後將以全新正式網址載入再確認一次穩定性。

最後一次以全新網址載入正式站亦成功：全球分數 -11、五因子與三市場比較完整、50 個 1H 實際觀察、1H／1D 控制項及 11:37 ET 最新快照均正常；沒有 HTML 解析錯誤、`Failed to fetch` 或骨架卡住。連同先前同源手動刷新已恢復按鈕的驗證，正式入口現以同源 JSON 路徑穩定提供資料。

走勢語意修正後，Cloudflare 公開全球頁以 `?interval=hour` 載入，文字明確顯示「每個小時保留最後一個實際分數」及「圖表覆蓋所有已保存歷史」。圖上有 27 個每小時觀察，時間橫跨 8 月 25 日至 8 月 26 日，證實 1H 已是資料點間隔而非最近一小時範圍；控制項只顯示「每小時／每日」。

切換到 `?interval=day` 後，Cloudflare 全球頁顯示「每天保留最後一個實際分數」、2 個每日觀察，以及 8 月 25 日、8 月 26 日兩個實際日期。再直接讀取每日 API，全球、香港、中國均各返回 2 個歷史點，日期同為 2026-08-25、2026-08-26；三市場均已按每日最後一個實際快照彙整。

Manus 正式網址完成新版本傳播後，以 `?interval=hour&build=338c966f` 載入正常：頁面顯示「每小時／每日」控制項、27 個每小時觀察，以及「每個小時保留最後一個實際分數；圖表覆蓋所有已保存歷史」的明確說明。同源資料載入成功，沒有 `Failed to fetch` 或 HTML JSON 解析錯誤。

同一正式網址的 `?interval=day&build=338c966f` 亦完成載入：顯示「每天保留最後一個實際分數」、2 個每日觀察，以及 8 月 25 日、8 月 26 日的真實快照紀錄。頁面資料口徑同時說明每小時／每日都是全部已保存歷史的資料點間隔，沒有用最近一天篩選或補造歷史。

最後以正式網址的同源 API 同時核對三市場：全球有 27 個每小時點和 27 個不同小時桶；香港有 26 個每小時點和 26 個不同小時桶；中國有 26 個每小時點和 26 個不同小時桶。三市場每日視圖均有 2 個點，日期均為 2026-08-25、2026-08-26。此結果確認每個資料點均來自已保存快照的最後觀察，並依選擇的每小時／每日桶正確彙整。

正式香港頁的 `?interval=hour&build=338c966f` 已完成介面驗證：顯示「每小時／每日」控制項、26 個每小時觀察，時間軸跨 8 月 26 日至 8 月 27 日 HKT；摘要文字為「每個小時保留最後一個實際分數」。頁面五因子為 EWH、VXX、HYG、UUP、KWEB，並保持來源與時間戳披露。

正式香港頁的 `?interval=day&build=338c966f` 亦完成載入：顯示「每天保留最後一個實際分數」、2 個每日觀察，最近結論包含 8 月 26 日與 8 月 27 日 HKT 的真實保存快照；控制項與資料口徑仍清楚說明兩種選擇皆使用全部已保存歷史。

正式中國頁的 `?interval=hour&build=338c966f` 已完成介面驗證：顯示「每小時／每日」控制項、26 個每小時觀察，時間軸跨 8 月 26 日至 8 月 27 日 HKT；摘要文字為「每個小時保留最後一個實際分數」。頁面五因子為 ASHR、VXX、HYG、UUP、KWEB，全部保留來源與時間戳披露。

正式中國頁的 `?interval=day&build=338c966f` 亦完成載入：顯示「每天保留最後一個實際分數」、2 個每日觀察，最近結論包含 8 月 26 日與 8 月 27 日 HKT 的真實保存快照；控制項和資料口徑明確表示兩種選擇均看全部已保存歷史。
